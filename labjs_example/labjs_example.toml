# Database
SQLALCHEMY_DATABASE_URI = 'sqlite:///labjs_example.db'
SECRET_KEY = 'You Must Change This to Something Unique'

# Application
TITLE = 'lab.js Example - Stroop Task with BOFS Backend'
ADMIN_PASSWORD = 'example'
PORT = 5007
APPLICATION_ROOT = ''
USE_BREADCRUMBS = true
RETRIEVE_SESSIONS = true
ALLOW_RETAKES = true
LOG_QUESTIONNAIRE_INTERACTIONS = false

# Single condition. Replace with a list of {label, enabled} entries to randomize.
CONDITIONS = []

# Completion
GENERATE_COMPLETION_CODE = true
COMPLETION_CODE_MESSAGE = 'Thanks for participating. Your completion code is:'

# Pages shown to the participant, in order.
# Demonstrates the complementary split: BOFS handles consent / questionnaires /
# page flow; lab.js runs the Stroop task in the browser and POSTs trial rows
# back to the BOFS-managed `labjs_trials` table. The Stroop page is a
# `custom/` page so lab.js takes over the full viewport with no BOFS chrome.
PAGE_LIST = [
    {name='Consent', path='consent'},
    {name='Demographics', path='questionnaire/demographics'},
    {name='Instructions', path='instructions/task_instructions'},
    {name='Stroop Task', path='custom/labjs_stroop'},
    {name='Post-task', path='questionnaire/post_task'},
    {name='End', path='end'}
]
